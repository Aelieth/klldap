query GetUserDetails($id: String!) {
  user(userId: $id) {
    id
    email
    avatar
    displayName
    creationDate
    uuid
    mfaEnrolled
    groups {
      id
      displayName
    }
    attributes {
      schema {
        name
      }
      value
    }
  }
  schema {
    userSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isEditable
        isHardcoded
        isReadonly
      }
    }
  }
}
